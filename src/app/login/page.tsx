export default function LoginPage() {
  return (
    <div className="flex min-h-full flex-col items-center justify-center bg-surface-page px-4 py-12">
      <p className="text-sm text-muted">Сторінка входу (без dashboard layout)</p>
    </div>
  );
}
